
(function(){
const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>Array.from(r.querySelectorAll(s)); const toast=t=>{const el=$('#toast'); el.textContent=t; el.style.display='block'; setTimeout(()=>el.style.display='none',2400)};
const mediaKey='elkass.media.v60'; const readMedia=()=>{try{return JSON.parse(localStorage.getItem(mediaKey)||'[]')}catch(e){return[]}}; const writeMedia=v=>localStorage.setItem(mediaKey,JSON.stringify(v||[]));
function tabs(){ $$('aside [data-tab]').forEach(b=>b.onclick=()=>{$$('aside [data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');$$('.tab').forEach(t=>t.classList.remove('active'));$('#'+b.dataset.tab).classList.add('active')}) }
async function getProducts(){ return window.ElkassCloud?.getProducts ? await window.ElkassCloud.getProducts() : [] }
function slug(s){ return String(s||'produkt').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ł/g,'l').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'') || 'produkt-'+Date.now(); }
async function renderProducts(){ const list=await getProducts(); $('#countProducts').textContent=list.length; $('#productList').innerHTML=list.map(p=>`<div class="row" data-id="${p.id}"><img src="${p.image||'/assets/products/product-05-telewizor-samsung.jpg'}"><div><b>${p.name}</b><small>${p.brand||''} • ${p.category||''} • ${p.price||0} zł</small></div><button type="button" data-edit="${p.id}">Edytuj</button><button type="button" data-delete="${p.id}">Usuń</button><a href="/app/product/?id=${encodeURIComponent(p.id)}" target="_blank">Otwórz</a></div>`).join(''); $$('#productList [data-edit]').forEach(btn=>btn.onclick=()=>{const p=list.find(x=>x.id===btn.dataset.edit); if(!p)return; const f=$('#productForm'); ['id','name','brand','category','price','oldPrice','badge','image','short'].forEach(k=>{if(f.elements[k]) f.elements[k].value=p[k]||''}); if(f.elements.features) f.elements.features.value=(p.features||[]).join('\n'); toast('Produkt wczytany do edycji');}); $$('#productList [data-delete]').forEach(btn=>btn.onclick=async()=>{if(!confirm('Usunąć produkt z lokalnej bazy?'))return; await window.ElkassCloud.deleteLocalProduct(btn.dataset.delete); toast('Produkt usunięty'); renderProducts();}); }
function formProduct(){ $('#productForm')?.addEventListener('submit',async e=>{e.preventDefault(); const fd=new FormData(e.currentTarget); const p=Object.fromEntries(fd.entries()); p.id=p.id||slug(p.name); p.price=Number(String(p.price).replace(',','.'))||0; p.oldPrice=Number(String(p.oldPrice).replace(',','.'))||undefined; p.features=String(p.features||'').split('\n').map(x=>x.trim()).filter(Boolean); p.images=[p.image].filter(Boolean); await window.ElkassCloud.saveProduct(p); toast('Produkt zapisany i widoczny na stronie'); renderProducts(); }); }
function renderMedia(){ const m=readMedia(); $('#countMedia').textContent=m.length; $('#mediaGrid').innerHTML=m.map((x,i)=>`<button data-i="${i}"><img src="${x.url}"><small>${x.name||'grafika'}</small></button>`).join(''); $$('#mediaGrid button').forEach(b=>b.onclick=()=>{$$('#mediaGrid button').forEach(x=>x.classList.remove('active')); b.classList.add('active')}); }
function mediaUpload(){ $('#mediaFile')?.addEventListener('change',e=>{const f=e.target.files[0]; if(!f)return; const r=new FileReader(); r.onload=()=>{const m=readMedia(); m.unshift({name:f.name,url:r.result,type:f.type,createdAt:new Date().toISOString()}); writeMedia(m); renderMedia(); toast('Grafika dodana do biblioteki');}; r.readAsDataURL(f);}); }
function selectedMedia(){ const b=$('#mediaGrid button.active'); if(!b)return null; return readMedia()[Number(b.dataset.i)] }
async function saveHomePatch(patch){ const settings=window.ElkassCloud.readLocalSettings ? window.ElkassCloud.readLocalSettings() : {}; const home=Object.assign({}, settings['home-final']||{}, patch); await window.ElkassCloud.saveSetting('home-final',home); localStorage.setItem('elkass.home.live.v59', JSON.stringify(home)); }
function homeForm(){ $('#homeForm')?.addEventListener('submit',async e=>{e.preventDefault(); const data=Object.fromEntries(new FormData(e.currentTarget).entries()); await saveHomePatch(data); toast('Strona główna zapisana');}); $('#useHero')?.addEventListener('click',async()=>{const m=selectedMedia(); if(!m)return toast('Najpierw zaznacz grafikę'); await saveHomePatch({heroImage:m.url}); toast('Grafika ustawiona jako Hero');}); $('#useSalon')?.addEventListener('click',async()=>{const m=selectedMedia(); if(!m)return toast('Najpierw zaznacz grafikę'); await saveHomePatch({salonImage:m.url}); toast('Grafika ustawiona jako Salon');}); }
function publish(){ $('#publish')?.addEventListener('click',()=>{toast('Zapisane. Otwórz / lub odśwież podgląd sklepu.'); setTimeout(()=>window.open('/','_blank'),400);}); }
document.addEventListener('DOMContentLoaded',()=>{tabs();formProduct();mediaUpload();homeForm();publish();renderMedia();renderProducts();});
})();
